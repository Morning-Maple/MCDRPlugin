# commands 子包入口
#   show.py     — !!mscm show [名字]                     (已实现)
#   sync.py     — !!mscm sync <名字>                     (已实现, 全量同步)
#   control.py  — !!mscm start/stop/restart <名字>       (已实现, 仅 Windows)
